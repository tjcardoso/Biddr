<%= form_for [@auction, @auction.bids.build] do |f| %>

  <p>
    <%= f.label :amount %><br>
    $ <%= f.text_field :amount %>
  </p>
  <p>
    <%= f.submit data: {disable_with: "Submitting..."}, class: "btn btn-primary" %>
  </p>
<% end %>
